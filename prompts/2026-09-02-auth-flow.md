# Lane AUTH — the /auth flow: sign-in, sessions, and the merge

**Brendan, 2026-09-02:** *"we likely need to set up an /auth flow at this
point."* He is right and the sequence has earned it: connections, Agentic
Inbox threads, and tasks all key to a per-browser token the surfaces honestly
call "not authentication." This lane makes it authentication. What it unlocks,
in his framing: connections and inbox threads follow YOU instead of a browser,
the seeded humans become real recipients, CC-to-people gains actual delivery,
and the close-the-tab inbox (v2) becomes buildable.

**Where this runs:** govblock (`~/Code/govblock`), live at
https://policy.nysgpt.com — Amplify WEB_COMPUTE app `d2a69zdzqun8m7`,
us-east-1, account 638175140432. READ FIRST: `prompts/2026-09-01-bedrock-agents.md`
§4 and the LEAD lines from 05:20Z onward — it holds what you inherit: the
/connectors surface, AgentCore Identity's token vault with workload identity
`govblock` (per-user tokens mint via `get-workload-access-token-for-user-id`
from the compute role's ordinary signed call — proven, and its `--user-id` is
exactly where your user identity will plug in), the per-browser claim check,
and the IAM statement (`AgentCoreIdentity` in `govblock-data-access`,
`govblock-*`-scoped). Platform limits that will shape you: Amplify buffers
whole responses AND cuts them at 30 s; the build GATES on type errors; the
local hook blocks tsc by name, so eyeball types before pushing.

## 0. Rules

1. **Files you own, and nothing else:** `apps/web/app/auth/**`,
   `apps/web/lib/auth/**`, the header's RIGHT SIDE account affordance (the one
   part §8 deliberately left untouched), auth-related IAM/infra, and — at the
   migration stage only — the storage-key merge described in §3. NOT yours:
   `lib/agents/**`, the /connectors page, the inbox block, the member pages —
   lane X (`lane-x`) is live in them on §10b/§11. The seam between you is the
   USER-ID CONTRACT: you publish one accessor (session → stable user id) and
   lane X consumes it later; agree the shape through the lead, not by editing
   each other's files.
2. Explicit-path commits; `git status` first; never `git add -A`. One slice
   per build. Verify on https://policy.nysgpt.com with Playwright at 1714 px.
3. Report below the marker in THIS file. `HEARTBEAT` every 45 min, `FLAG:`
   for rulings (keep going), one `LANE AUTH STATUS: COMPLETE | PARTIAL |
   STOPPED — <why>` at the end.
4. Secrets never in the repo (it is public). Brendan's Google OAuth client id
   and secret are landing in `~/Code/livingston/.env.local` or
   `apps/web/.env.local` for the Drive connector — sign-in may share or split
   that client per your §1 finding. Cost lines follow the house rule: an
   under-reporting cost line is worse than none.

## 1. The evaluation — 30 minutes each, evidence, recommendation BEFORE code

- **(a) Amazon Cognito** — the AWS-standard lean Brendan has mandated before:
  user pool + Google as federated IdP, hosted UI or Amplify Auth libs.
  Measure against OUR constraints: does the hosted-UI redirect flow play with
  policy.nysgpt.com; token handling in SSR under the buffering/30 s limits;
  and the prize question — does Cognito's `sub` slot cleanly into the
  workload-identity `--user-id` the vault already keys on? If yes, sign-in and
  per-user Drive grants become one coherent identity with zero glue.
- **(b) Auth.js (NextAuth) with Google** — in-app, fewer AWS moving parts;
  the same Google client could serve sign-in (openid/email/profile) AND the
  Drive connect grant (drive.file). Name plainly whether sharing one client is
  wise or sloppy.
- Probe by DOING (a minimal flow or a measured mock), not by docs alone —
  ListFoundationModels taught this account that catalogues lie. Reject or
  adopt with reasons; the lead's prior is Cognito for stack coherence,
  overridden happily by evidence.

## 2. The staged build, after the ruling

1. Sign-in: Google first, email second. /auth surface (sign-in/out) in the
   site's own design vocabulary — port, don't invent.
2. Session reaches SSR (the policy routes and agents routes can read it), and
   the header gains the small account affordance on its right side.
3. Publish the user-id contract (one accessor; document it in this file for
   lane X).

## 3. The merge — the part that must be honest

Everything keyed to the browser claim check (inbox threads, connections,
tasks) gains a user key. On FIRST sign-in, one visible, one-time offer:
"this browser's history becomes yours" — never silent adoption. The claim
check remains the anonymous fallback for signed-out visitors, and every
"kept in this browser" sentence on the surfaces updates to tell the new truth
when signed in (coordinate copy changes on lane X's surfaces through the
lead). Peter Parker and Tony Stark stay seeded beside real users.

## 4. Expected Brendan FLAGs

The Google client gains the sign-in redirect URI for whichever path wins
(Cognito's hosted domain or /api/auth/callback/google), and Testing-mode
test-user emails. Hand him exact clicks, build behind honest states until
they exist.

---

## Report — worker appends below this line
