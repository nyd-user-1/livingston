# Lane B — the Congressional Budget Office: audit for content and suitability

**Brendan, 2026-09-01 ~17:10Z:** *"The Congressional Budget Office (CBO) does
not offer a traditional dedicated REST API, but instead provides
machine-readable data through its Open Data Repository on GitHub and RSS
feeds … audited for both content and suitability … a full accounting."*
**Discovery only** — no tables, no pipeline, no pages; the deliverable is a
decision-ready accounting in the shape of `prompts/2026-09-01-congress-api.md`
§1 that Brendan can approve a harvest from.

**Sources, all three:**
1. **Open Data Repository** https://github.com/US-CBO/cbo-data — 13 datasets
   in `catalog.json` (measured 17:05Z): historical data and economic
   projections, long-term economic projections, potential GDP, demographic
   projections, historical budget data, 10-year and long-term budget
   projections, trust funds, revenue by category, spending by budget account
   (~2,000 accounts, wide format), automatic stabilizers, tax parameters.
   Long-format CSVs with `schema.json`s and vintages. Read `LICENSE.md`.
2. **Per-bill cost estimates** — the thing a bill page actually wants. They
   are publications on cbo.gov (which answers `curl` with 403 — bot
   protection; measure what a real `User-Agent` and the RSS endpoints get you,
   and say plainly if the site is off-limits to a harvester). The
   **BILLSTATUS zips govinfo publishes carry `<cboCostEstimates>` per bill**
   (title, URL, pubDate, description) and are already on box 2 from lane C's
   §5.1 pass — count how many 119th-Congress bills carry one, and whether the
   linked PDF/HTML is fetchable.
3. **RSS feeds** — find the real feed URLs (cbo.gov/rss and per-topic feeds:
   cost estimates, reports, blog), what each carries (title, date, link,
   summary, bill number?), and how far back they go.

## 0. Rules

Read-only; explicit-path commits of this file only; every number from a
request you made, with the command; polite pace against cbo.gov (one request
every few seconds, a real User-Agent, stop on any block); same reporting
protocol — `HEARTBEAT` every 45 min, `FLAG:` for rulings, one
`LANE B STATUS: COMPLETE | STOPPED — <why>` at the end.

## 1. The accounting — what is there

Per dataset in the repository: rows, columns/variables (from `schema.json`),
vintages available, date span, update cadence (from `CHANGE_LOG.md`), size.
Per-bill cost estimates: how many exist for the 119th (from the zips, and from
whatever cbo.gov/RSS yields), what one contains (the budget-impact table,
direct spending/revenues/deficit by year, mandate statements), and whether
the numbers are extractable or PDF-only. RSS: feeds, fields, span, cadence.

## 2. The accounting — suitability for govblock

With evidence: which surfaces this feeds — a bill page's **Cost estimate**
section (the obvious one), a **Budget** surface (10-year projections, revenue
and spending by account — the `spending_detail` accounts join to agencies and
to appropriations bills?), the newsroom's Congress desk (new cost estimates
as they publish), member pages (nothing — say so). Licence and attribution
terms. Harvest cost: the repository is a `git pull` — say how big; cost
estimates are one fetch per bill with one; RSS is free. Storage at Lane A's
Parquet ratios.

## 3. Recommendation

One table: family · rows available · rows worth holding · why · page it
would feed · cost · risk; then harvest now / later / not, per family.

---

## Report — worker appends below this line
