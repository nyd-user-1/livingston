# HANDOFF — 2026-08-28, the day livingston went national

**For:** the next lead session (Fable) and any lane window. **Written by:** the lead, 2026-08-29 00:40 ET, before compaction.
**Memory files carry the same facts** (`policy-neon-legislative-data`, `direct-pipeline-decision`, `product-directions-2026-08-28`, `portfolio-record-ledger`, `livingston-form-chat-lanes`, `infra-cost-actions-2026-08-28`).

## Where things stand

**Policy database (Neon `policy-nysgpt`, `POLICY_DATABASE_URL`):** 91 tables, **153.0M rows, 41 GB**. 2,128,806 bills across 52 jurisdictions, every session LegiScan holds (998 archives, all hashed in `"LegiscanDatasets"`); 4.37M document links (key is `(document_type, document_id)` since `ebb1337` — the old single key had silently destroyed 27% of the table); `"BillTexts"` 617k texts / >3.2B chars — **NY complete** (9 sessions, 246k versions, 99k memos, via the Senate API's bulk listing: 236 requests), **Congress complete** (govinfo BILLS 111th–119th + BILLSUM CRS summaries 113th–119th, 87,714), NJ ~19k of 107k and walking, other 49 states current+prior session via the nightly walker (~4.5 days, Arizona's 30 s crawl-delay bounds it); LDA lobbying 2023–2026 complete (332k filings, 558k bill links, 99.8% resolved); OpenFEC basic + extras for all 726 crosswalked members; model bills (ALEC 1,128 texts; CPI 545 resolved matches, 299 with text; NCSL refused — robots.txt disallows ClaudeBot). FTM: built, **quota-bound (1,000 rows/yr)**, never batch it.

**Portfolio ledger (one unit — rows as they would load):** databases 269,978,742 + harvested-not-loaded 560,853,569 (Aetna FHIR 502.9M on S3 `leuk-aetna-bulk-…` and `/mnt/aetna` on the 44b box; CMS Part D 16.4M; openFDA 14.2M; ClinicalTrials 14.1M rows from 597,913 studies; NIH RePORTER 13.2M) = **830,832,311** (+ NJ text creeping). The nysgpt.com `LEDGER` in `NYSgpt/lib/home-data.ts` is hardcoded (646.7M) and should be updated to this with the rule stated. Aurora `aurora-2525` is empty and not reachable outside its VPC — it was the intended Aetna load target.

**Boxes.** Box 1 `i-030d9cac100e6e124` (44b-worker, key `~/.ssh/44b-worker.pem`): livingston checkout at `~/livingston`, six `lv-*` manifests (`ops/box/jobs.d`), wake 07:15 UTC (`44b-wake-nightly`). Box 2 `i-0843042df1a5fb003` (`livingston-worker-2`, key `~/.ssh/livingston-worker-2.pem`, deploy key 161628655): seven `dp-*` manifests (`ops/box2/jobs.d`), wake 07:45 UTC (`livingston-worker-2-wake-nightly`, role `livingston-scheduler-wake-box-2`), boot unit + catch-up timer enabled, `.env.local` = `POLICY_DATABASE_URL NYS_LEGISLATION_API_KEY CRON_SECRET NEW_YORK_API_KEY`. Both stopped by default; `run-job`/`run-due` carry `JOB_CWD`/`"repo"` (44b `ce56b78`). **Gotchas:** touch `~/.no-auto-jobs ~/.keep-up` within ~2 min of boot or `run-due --boot` stops the box; a released hold leaves no janitor (stop by hand); `--region us-east-1` always; the worker-box windows run under the other Claude account, so peer messaging from this session does not reach them.

**Vercel:** no crons left in `vercel.json`; the `api/*` routes stay callable with `CRON_SECRET`. sports/44b/tariffs are paused by Brendan; caching commits await deploy on unpause.

## Owed

1. **Rotate the NY Senate API key** (leaked to a log by Open States' scraper, contained): new key → laptop `.env.local`, both boxes, Vercel.
2. NJ text: check `"BillTexts"` NJ count vs 107,573 at the next wake; hand-restart `lv-text-nj` (`JOB_CWD=$HOME/livingston ~/bin/run-job lv-text-nj node scripts/box/text-backfill.mjs --source state_link --state NJ --since-session 2010 --batch 400`) if short.
3. After the first full walk: `text-backfill.mjs --retry-errors`; re-run BT's proof (d) (search timing) at full size; six TLS-broken hosts got a CA bundle — verify their counts.
4. `VACUUM` policy DB when quiet; re-check size (41 GB, much is bloat from the rebuild).
5. WORKER-BOX.md: add the released-hold/janitor gap and the boot-flag timing; report-due should print `run-due md5 · repo shas` (lane WB §12).
6. Two 44b jobs failing on the box for weeks (`openreview` bad `DATABASE_URL`, `hf-sweep` missing flag) — 44b's to fix; eight manifests in repo vs six installed.
7. Decide: load the 560.9M harvested rows (Aurora exists, empty; `COPY` from S3); drop the $24/mo EBS duplicate once S3 is verified against the count file.
8. Not started, decided: **model lane** (Tier 1 encoder/reranker on self-labeled pairs incl. model-bill clusters; Tier 2 calibrated odds-and-reasons; Tier 3 "Livingston-8B" CPT+SFT on one g6e.12xlarge — G quota 64 vCPU, P quota 0) and the **search lane** (Postgres FTS on `search_tsv` first, measured warm/cold; Typesense/Manticore on a box only if needed). Products: the entity "spine" + Livingston workspace, public-money graph, **Penny forms-as-a-service (keeper)**, tariffs, health-rate intelligence.
9. Parked round-3 form-chat items (`prompts/PARKED-round-3.md`) — only when Brendan opens the round. Lane B (policy app rewrite) — on request.

## Lane files (brief + Report in each)
`prompts/2026-08-28-worker-box.md` (WB) · `-bill-text.md` (BT, with Resume) · `-independence.md` (IN, closed) · `-direct-pipeline.md` (DP) · `-model-bills.md` (MB) · `-forms-rail.md`, `2026-08-27-form-chat-ui*.md` (UI lanes, done). Doctrine: `~/Code/scripts/{RIG,ORCHESTRATION,SCRAPER}-DOCTRINE.md`.

## Rules Brendan set today (don't re-litigate)
Fable builds directly when told "complete all of this work"; otherwise writes prompts, monitors, Q/As, pushes. LegiScan stays canonical, maybe forever; the 52 direct loaders are a fail-safe exercised nightly — **no promotion, no cutover**. Count records as relational rows, always. Politeness is never tuned down. Push only after Q/A (he says go). He tests in the browser himself.
