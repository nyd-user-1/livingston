# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel

# Supabase CLI scratch — local project refs and version pins
supabase/.temp/

# Bulk data drops (NSR dumps etc.) — hundreds of MB, never committed.
# Root-anchored: public/data/ (pre-rendered app JSON) IS committed.
/data/
.env*
!.env.example
.cutover-backup

# Nested lane worktrees (harness default) — never commit as gitlinks
.claude/worktrees/

# Research scratch — walk captures, PDF teardown, egress scaffold
.research/
